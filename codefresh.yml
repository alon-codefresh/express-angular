version: '1.0'
steps:

  build_project:
    type: build
    description: codefresh example
    working_directory: ${{main_clone}}
    image-name: express-angular-test
    dockerfile: Dockerfile
    tag: hello

  push-to-dockerhub1:
    type: push
    candidate: ${{build_project}}
    registry: dockerhub
    tags: [ 'hello', 'master', lalala ]
