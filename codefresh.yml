version: '1.0'
steps:

  build_project:
    type: build
    description: codefresh example
    working_directory: ${{main_clone}}
    image-name: aloncodefresh/express-angular-test4
    dockerfile: Dockerfile
    tag: hello

  push-to-dockerhub1:
    type: push
    candidate: ${{build_project}}
    registry: dockerhub
    tags: [ 'hello4', 'master4', lalala4 ]
