version: '1.0'
steps:

  build_project:
    type: build
    description: codefresh example
    working_directory: ${{main_clone}}
    image-name: aloncodefresh/express-angular-test3
    dockerfile: Dockerfile
    tag: hello

  push-to-dockerhub1:
    type: push
    candidate: ${{build_project}}
    registry: dockerhub
    tags: [ 'hello3', 'master3', lalala3 ]
