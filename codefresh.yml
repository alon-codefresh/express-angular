version: '1.0'
steps:

  build_project:
    type: build
    description: codefresh example
    working_directory: ${{main_clone}}
    image-name: ncodefresh/express-angular-test
    dockerfile: Dockerfile
    tag: hello

  push-to-dockerhub1:
    type: push
    candidate: ${{build_project}}
    registry: docker-hub
    tags: [ 'hello', 'master', lalala ]
